let mapleader = ","
let g:instant_markdown_slow = 1

" Since system-wide plugin conflicts/doesn't protect itself:
call janus#disable_plugin('nodejs')

call janus#disable_plugin('tabbar')

" Fix easymotion leader key
let g:EasyMotion_leader_key = '<Leader>'
let g:erlang_tags_ignore="'_rel/**'"

